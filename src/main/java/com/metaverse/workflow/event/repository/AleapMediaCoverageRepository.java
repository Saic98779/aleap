package com.metaverse.workflow.event.repository;

import com.metaverse.workflow.model.AleapMediaCoverage;
import org.springframework.data.jpa.repository.JpaRepository;

public interface AleapMediaCoverageRepository extends JpaRepository<AleapMediaCoverage,Long> {
}
